import numpy as np
import math
    
class TrajectoryPlanner:
    def __init__(self, start_point, end_point, max_velocity, max_acceleration):
        self.start_point = start_point
        self.end_point = end_point
        self.max_velocity = max_velocity
        self.max_acceleration = max_acceleration

    def calculate_ux_trajectory(self) -> np.ndarray:
        print("Calculating ux trajectory...")
        return np.array([])

    def calculate_uy_trajectory(self) -> np.ndarray:
        print("Calculating uy trajectory...")
        return np.array([])
        
    def calculate_uz_trajectory(self) -> np.ndarray:
        print("Calculating uz trajectory...")
        return np.array([])

    def calculate_x_trajectory(self) -> np.ndarray:
        print("Calculating x trajectory...")
        return np.array([])

    def calculate_y_trajectory(self) -> np.ndarray:
        print("Calculating y trajectory...")
        return np.array([])

    def calculate_z_trajectory(self) -> np.ndarray:
        print("Calculating z trajectory...")
        return np.array([])

    def calculate_linear_trajectory(self) -> np.ndarray:
        print("Calculating arbitrary linear trajectory...")

        return np.array([])