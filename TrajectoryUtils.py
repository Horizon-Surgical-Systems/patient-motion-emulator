import numpy as np
import math

class TrajectoryUtils:
    @staticmethod
    def trajectory_smoothing(trajectory: np.ndarray) -> np.ndarray:
        print("Smooth trajectory...")
        return trajectory

    @staticmethod
    def trajectory_padding(trajectory: np.ndarray) -> np.ndarray:
        print("Padding trajectory...")
        return trajectory